spring.application.name=bfhl

# Render assigns a dynamic port, fallback to 8080 locally
server.port=${PORT:8080}

# Important: Bind to all network interfaces, not just localhost
server.address=0.0.0.0
